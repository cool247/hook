import { TextField, Button, LinearProgress, Grid } from '@material-ui/core';

import { Autocomplete } from '@material-ui/lab';

export const StyledTextField = function () {
  return <TextField />;
};
export const StyledButton = function () {
  return <Button />;
};

export const StyledGrid = function () {
  return <Grid />;
};
export const StyledLinearProgress = function () {
  return <LinearProgress />;
};
export const StyledAutocomplete = function AutocompleteAutocomplete() {
  return <Autocomplete />;
};
